<div class="accordion" id="accordionExample">
  <div class="container">
    {{#each resData}}
      <div class="card mb-1">
        <div class="card-header" id="headingOne">
          <h2 class="mb-0">
            <button
              class="btn btn-link"
              type="button"
              data-toggle="collapse"
              data-target="#{{this.nip}}"
              aria-expanded="true"
              aria-controls="{{this.nip}}"
            >
              {{this.name}}
            </button>
          </h2>
        </div>
        <div
          id="{{this.nip}}"
          class="collapse"
          aria-labelledby="headingOne"
          data-parent="#accordionExample"
        >
          <div class="card-body">
            NIP:
            {{this.nip}}
            <br />
            <br />
            Residence address:
            {{this.residenceAddress}}
            <br />
            <br />
            Working address:
            {{this.workingAddress}}
            <br />
            <br />
            Status:
            {{this.status}}
            <br />
            <br />
            Accounts:
            {{#each this.accountNumbers}}
              <li>
                {{this}}
              </li>
            {{/each}}
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>